<div id="tableheader">
<h1>Manage Route</h1>
</div>

<table>
  <tr>
    <th>No</th>
    <th>Nama</th>
    <th>Manage</th>
  </tr>

<% @salesmen.each do |salesman| %>
  <tr>
    <td><%= salesman.id %></td>
    <td><%= salesman.name %></td>
	  <td>
      <%= link_to 'Create', {:controller=>"assignroutes", :id=>salesman.id} %> |
      <%= link_to 'List', {:controller=>"routelist", :salesman_id=>salesman.id} %></td>
  </tr>
<% end %>
</table>
<br />
